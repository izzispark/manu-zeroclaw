[package]
name = "zeroclaw-apply-overrides"
version = "0.1.0"
edition = "2021"

[dependencies]
toml = "0.8"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
walkdir = "2"
